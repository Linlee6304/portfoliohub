# Development: Vite forwards /api to ASP.NET, without browser CORS configuration.
API_PROXY_TARGET=https://localhost:7051
# Production: serve /api on the same domain via a reverse proxy.
VITE_API_BASE_URL=/api
